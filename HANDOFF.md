# GentleTap — Handoff (Agent change)

Project: **Merge old GentleTap into the new project** (keep new blue design + Vite SPA + org model).

## Repository layout
- **Base (new project, where you do all work):** `C:\Users\HomePC\Music\2-gentletap`
  - `backend/` — FastAPI app (python, package `app/`)
  - `frontend/` — Vite + React SPA (blue design, Tailwind v3, Inter, lucide-react, brand blue `#2563eb`)
  - `docker-compose.yml` — api / web (nginx) / worker / beat / redis / postgres
  - `gentletap-main (1)/gentletap-main` — the **old** project; this is the *source* we copy code from and adapt.
- Branch is `main`. VPS is at `/opt/gentletap`, updated with:
  `cd /opt/gentletap && git pull origin main && docker compose up -d --build`

## How we work
- Do work in phases (fully listed below). **Commit + push after each phase** so the VPS can be updated incrementally.
- When copying old files, first copy them into the new tree, then adapt imports:
  - `gentletap.` → `app.`
  - `user_id` references get re-mapped to `org_id` (new model is org-scoped).
  - All new pages/components must use the **blue design system** (Tailwind v3, Inter, lucide-react, `#2563eb`).
- Alembic migrations are used for schema changes. **They are intentionally idempotent** (they inspect the DB and skip existing tables/columns) so they run cleanly on the existing VPS database and on fresh databases. Keep new migrations idempotent.

---

## Progress summary

| Phase | Status | What was delivered |
|---|---|---|
| 1. Deployment hardening | ✅ Pushed `b625a75` | Production frontend build (multi-stage Dockerfile: npm ci → vite build → nginx:alpine serving `dist/` with SPA fallback + `/api` proxy to `api:8000`). nginx security/gzip headers. Fixed login/signup proxy (VITE_API_URL / nginx proxy → `api:8000`). Redis/Celery worker fixes (broker_connection_retry_on_startup, celerybeat-schedule filename, result_expires, task expires + time limits). Auto-migrations on API startup (alembic upgrade head, SKIP_DB_MIGRATIONS override). Compose hardening (worker/beat healthchecks, remove worker container_name to allow `--scale`, ports bound to `127.0.0.1`, removed public Postgres port). Fixed Google OAuth redirect mismatch. |
| 2. Compliance & recovery | ✅ PUSHED `dad7a10` | Forgot/reset password flow (Redis tokens + email). Forgot-password / reset-password pages (blue design). Legal pages (privacy, terms, cookies, refund) in footer. Scrub real secrets from `.env.example`. |
| 3. Core features | ✅ PUSHED `1ba7658` | Scheduled OAuth token refresh for QB/FreshBooks/Google (sync no longer silently dies). `/analytics` endpoint + full Analytics page with charts. `/usage` metering endpoint + plan-limit enforcement. Notification system (model, migration, API, bell dropdown with unread counts). |
| 4. Admin & power features | ✅ PUSHED `6d7aa48` | Escalation rules CRUD (`/escalation-rules`, org-scoped, max 25 rules) + Escalations page rules panel. Admin API extended (`/admin/users` paginated, `/admin/audit-log`). |
| Fix — API container crash | ✅ PUSHED `27dd13c` | Migrations made idempotent (skip existing tables/columns). On the VPS this fixed a crash where `phase2_001` tried to re-create tables that already existed from the app's `create_all` startup. **Note:** the backend `lifespan` still calls `Base.metadata.create_all` on startup AND the Docker entrypoint runs `alembic upgrade head` first. Keep these two in sync / idempotent, or you will re-trigger this exact crash. |
| 5a. SEO / marketing | ✅ PUSHED `6d74f6f` | Ported all old SEO content data files to `frontend/src/data/` (blog-posts ×9, competitor-comparisons ~17, industries ×10, features ×6, seo-content FAQs/timelines/templates). New blue-design routes: `/blog(/:slug)`, `/compare(/:slug)`, `/industries(/:slug)`, `/features(/:slug)`, `/alternatives`, plus `/quickbooks-invoice-automation`, `/quickbooks-reminders-vs-gentletap`, `/how-to-follow-up-on-overdue-invoices`. Shared `<Seo>` component (react-helmet-async) with canonical/OG + JSON-LD (Article, FAQ, HowTo, Breadcrumb). Dynamic sitemap.xml generated by Vite plugin (`vite.config.ts` closeBundle → dist/sitemap.xml, 40 URLs; old static public/sitemap.xml deleted). Cookie-consent banner (localStorage-gated, essential-only messaging). Landing footer + marketing header nav link everything. |
| 5b. Affiliate program | ✅ PUSHED `a98d8f9` | Backend: 6 models (`app/models/affiliate.py`) org-scoped referrals (`AffiliateReferral.org_id` unique), services (tiered commissions, clawbacks, payouts, dashboard aggregation), separate affiliate JWT auth (PyJWT, refresh-token family rotation w/ reuse grace), router under `/api/v1/affiliates/*` incl. admin endpoints gated by `X-Admin-Api-Key` header (**admin panel UI intentionally deferred** — API only for now). Paddle webhook hooks: commissions on transaction.completed/paid, clawback on adjustment refund events. Signup accepts optional `ref_code`; login calls attribute endpoint. Idempotent migration `phase5_001_affiliates` (+ `organizations.referred_by_affiliate_id`). Config gained tier/payout settings. Frontend: `/affiliates` landing + apply form, `/affiliates/login`, `/affiliates/dashboard` (ref link copy, stats, referrals/commissions/payouts tables), `/affiliates/terms`; `AffiliateRefTracker` mounted app-wide captures `?ref=` into cookie + track-click. |
| 5c. Intelligence engine | ✅ PUSHED `83ecfc2` | Ported `intelligence/` package to `backend/app/intelligence/`: risk scorer (two-tier), tone selector, timing optimizer (send windows), channel selector, escalation advisor, prompt builder, decision engine, schemas — pure logic near-verbatim. Rewrote `message_generator` onto existing AI layer: Kimi (priority model for pro_plus/team) → Z.AI → static template fallback; WhatsApp path uses `render_whatsapp_body`. New provider-agnostic `profiler.py` computes ClientProfile stats from local invoices (paid_at vs due_date) instead of live QB queries. `context_builder.py` maps new models → ReminderContext. API: `GET /api/v1/intelligence/preview/{invoice_id}` (full decide() draft preview + WhatsApp variant) and `GET /api/v1/intelligence/recommendations` (org-scoped open invoices). Also fixed latent config bug: added `kimi_timeout_seconds`/`zai_timeout_seconds` settings (kimi.py referenced them but they were undefined); `call_kimi` gained optional model override. |
| Fix — Login/signup not reaching dashboard | ✅ PUSHED `944f963` | CORS switched to wildcard with `allow_credentials=False` (auth is Bearer-header only — no cookies — so this is safe) eliminating origin-allowlist deploy failures. Dev auto-auth fallback in `get_current_user_and_org` now raises 401 in production instead of silently authenticating as demo user. Login/Signup do a hard `window.location.replace('/dashboard')` after verifying the token hit localStorage — ProtectedRoute always re-hydrates from storage, so no in-memory state edge case can bounce back to /login. **Note:** if the live site still misbehaves after deploy, rebuild the web container — a stale `dist/` was a leading suspect. |
| Admin dashboard | ✅ PUSHED `0fc22be` | `/admin` page (login-protected): Overview (orgs/users/MRR/messages-today stats + active connections), Affiliates tab (filter by status, approve/reject/pause/reactivate, inline payout form), Organizations, Users, Audit log. Backend admin auth made flexible: `require_admin_flexible` accepts either `X-Admin-Api-Key` header (server ops) or a valid JWT whose email ∈ ADMIN_EMAILS (dashboard UI). New `GET /admin/access-check` gates the UI. `/admin/stats` now includes total_users. Affiliate admin endpoints moved onto the flexible dep too. |
| 6. Intelligence autopilot + WhatsApp inbound log | ✅ PUSHED `04de93e` | `process_single_reminder` now runs every due reminder through the intelligence engine before sending: `should_send()` gating (client replied → reschedule +24h; dispute/paused/no-contact → skip with `intel_*` reason), `should_escalate()` → skips send, writes AuditLog `reminder_escalated` + owner notification "Human handoff recommended", risk-aware tone override (`score_risk` → `select_tone`) applied when drafting fresh. All intelligence failures are non-blocking (log + proceed). WhatsApp depth: new `WhatsappInboundMessage` model + idempotent migration `phase5_002_whatsapp_inbound`; Twilio webhook persists every inbound message (org/client matched by phone, opt-out flagged, SID-deduped); `context_builder` computes `client_responded_recently` from the inbound log + replied outbound messages, and `sequence_paused` from SequenceAssignment status. |
| 7. Final polish — reprofiling, timing, llms.txt | ✅ PUSHED `7e65c0e` | Weekly client re-profiling on Celery beat: new `app/tasks/profile_refresh.py` (`reprofile_all_orgs`, per-org isolation) scheduled Sundays 05:00 UTC (`reprofile-clients-weekly`, expires 6h). Timing-optimizer enforcement: step-1+ reminders landing outside the business-hours weekday window (weekends / before 08:00 local) are rescheduled via `next_send_window()` — step 0 still fires immediately. Ported `llms.txt` to `frontend/public/` (fixed stale routes: no `/integrations/*`, no `/contact`, priority AI = Kimi not GPT-4o) and re-added it to SITEMAP_PATHS (41 URLs). |
| 8. Security hardening & optimization audit | ✅ PUSHED 7ff7717 | **Secrets scrubbed from code** (all were live in git history — ROTATE ON VPS): Paddle webhook secret, Resend webhook secret, Intuit verifier token, token-encryption key, guessable admin API key (`gentletap_admin_key_prod`; also fixed its alias bug — it read SECRET_KEY instead of ADMIN_API_KEY). All now default empty and **fail closed**: Paddle/Intuit/FreshBooks webhooks reject when secrets unset; admin header auth disabled until ADMIN_API_KEY set; Resend webhook verifies Svix HMAC when secret set (warn-accepts when unset — low-risk endpoint); Twilio inbound validates X-Twilio-Signature when TWILIO_AUTH_TOKEN set. **Tokens encrypted at rest**: new `services/crypto.py` (Fernet, `enc:v1:` prefix, backward-compatible plaintext reads, mock pass-through) applied to all Connection token writes/reads incl. refresh task. **Rate limiting** (`services/rate_limit.py`, Redis-backed, fail-open): signup 5/min, login 10/min, refresh 30/min, forgot-pw 5/5min, reset-pw 10/5min, affiliate apply 5/min + track-click 120/min + affiliate login 10/min. **Startup warnings**: lifespan logs CRITICAL in production for unset critical secrets; new `backend/scripts/security_check.py` audits the VPS env (run: `docker compose run --rm api python scripts/security_check.py`). Login page demo-credential prefill removed. **Bundle split**: vendor-react/vendor-chunks manualChunks cut main chunk 1076KB → ~571KB. |
| 9. Env alignment — zero hardcoded credentials | ✅ THIS COMMIT | Audited the owner's real .env variable names against config.py; closed every gap both directions. **Removed ALL remaining credential-ish fallbacks**: SECRET_KEY/JWT_SECRET_KEY no longer default to placeholder strings; POSTGRES_PASSWORD/REDIS_PASSWORD example passwords removed; Paddle price-ID catalog values scrubbed (owner's .env supplies them). Composite URLs (DATABASE_URL, REDIS_URL, CELERY_*) are now built from POSTGRES_*/REDIS_* parts when not set explicitly. New model_validator: **production refuses to boot** without SECRET_KEY/JWT_SECRET_KEY/POSTGRES_PASSWORD/REDIS_PASSWORD (RuntimeError names the missing vars — verified); non-production auto-generates ephemeral dev keys. **Config gained fields the .env already had but code ignored**: OPENAI_API_KEY/MODEL/MODEL_PRIORITY (+ new services/ai/openai_client.py wired as FIRST provider in the reminder AI chain: OpenAI → Kimi → Z.AI → static), SENTRY_DSN, API_PROXY_URL. docker-compose.yml fallbacks ${VAR:-default} replaced with required-var syntax so compose fails fast on an incomplete .env. .env.example updated. |

Git log (newest → oldest):
```
<env-sha> Env alignment: require .env credentials, OpenAI provider chain, config gaps
<this-sha> Security hardening: secret scrub, fail-closed webhooks, token encryption, rate limits
<phase7-sha> Polish: weekly reprofile task, timing-optimizer enforcement, llms.txt
<phase6-sha> Phase 6: intelligence autopilot in reminder pipeline + WhatsApp inbound log
0fc22be Admin dashboard: /admin UI with JWT-or-API-key admin auth
944f963 Fix login/signup not reaching dashboard (CORS, dev-fallback gate, hard nav)
<phase5c-sha> Phase 5c: intelligence engine behind AI provider layer + preview API
a98d8f9 Phase 5b: affiliate program (org-scoped backend + creator portal frontend)
6d74f6f Phase 5a: SEO marketing pages, dynamic sitemap, cookie consent
27dd13c Fix API container crash: make alembic migrations idempotent
6d7aa48 Phase 4: escalation rules CRUD + admin users/audit-log endpoints
1ba7658 Phase 3: token refresh, analytics, usage metering, notifications
dad7a10 Phase 2: password recovery, legal pages, secret cleanup
b625a75 Phase 1: production deployment hardening
```

---

## PENDING FIX — Login / Signup not reaching the Dashboard

**Symptom (reported by owner):** after a user logs in or signs up on the live site, the site does **not** take them to `/dashboard`; it stays on the login page.

### Code walk-through so far (already diagnosed)
- `frontend/src/pages/Login.tsx` on submit:
  ```ts
  const res = await api.post('/auth/login', { email, password });
  const data = res.data;
  setAuth({ user: {...}, orgId: data.org_id, orgName: data.org_name, plan: data.plan,
             accessToken: data.access_token, refreshToken: data.refresh_token });
  navigate('/dashboard');
  ```
  If the request succeeds, it **always** navigates to `/dashboard`. So "stays on login" means one of:
  1. `api.post('/auth/login', ...)` **throws** (network / CORS / wrong URL) → caught → `setError(...)` and the page stays. (But then the user would see an error banner — ask/verify whether they see an error.)
  2. `navigate('/dashboard')` runs but `ProtectedRoute` (App.tsx) immediately redirects back to `/login`.
- `frontend/src/stores/authStore.ts` — `setAuth` writes `gentletap_access_token` / `gentletap_refresh_token` / `gentletap_org_id` / `gentletap_org_name` / `gentletap_plan` / `gentletap_user` to localStorage and sets `isAuthenticated: true`.
- `frontend/src/App.tsx` — `ProtectedRoute` checks `isAuthenticated` from `useAuthStore()`; if falsy → `<Navigate to="/login" replace />`. `/dashboard` is inside `<Layout/>` protected route.
- `frontend/src/lib/api.ts` — axios baseURL `'/api/v1'`, request interceptor reads `gentletap_access_token` from localStorage. **Note:** there is an empty `if (error.response?.status === 401)` slot for refresh logic that is intentionally not yet implemented.
- `backend/app/api/auth.py`:
  - `POST /login` returns `TokenResponse` with exactly the camelCase fields the frontend reads: `user_id`, `org_id`, `org_name`, `plan`, `access_token`, `refresh_token`, `full_name`.
  - `POST /signup` auto-creates an `Organization`, `OrganizationMember` (role owner), onboarding, then returns the same `TokenResponse`.
- `backend/app/schemas/auth.py` — `TokenResponse` fields confirmed: access_token, refresh_token, token_type, expires_in, user_id, email, full_name, org_id, org_name, plan.
- Routing: frontend `api` baseURL `/api/v1` → nginx (`frontend/nginx.conf`) proxies `location /api/` to `http://api:8000`, and `main.py` includes auth router at prefix `/api/v1`. So the full path is `/api/v1/auth/login`. This is correct.

### DIAGNOSIS COMPLETE — results (verified live on 2026-08-23)
Everything server-side is healthy RIGHT NOW:
- `POST https://gentletap.co/api/v1/auth/login` → **HTTP 200**, returns every field the frontend reads (`user_id`, `org_id`, `org_name`, `plan`, `access_token`, `refresh_token`).
- `POST /api/v1/auth/signup` → **HTTP 200** (created throwaway account agenttest9182@gmail.com).
- `POST /api/v1/auth/refresh` → **HTTP 200** (rotated tokens fine).
- CORS preflight OPTIONS passes for gentletap.co / www / localhost:3000; actual POST carries `access-control-allow-origin`. **Note: in production the SPA calls `/api/...` same-origin through nginx, so CORS never even applies there.**
- The deployed bundle (`/assets/index-C_yGzAdH.js`) was **byte-identical (md5 c8f2dca3600c1a05bef3da1c0570fa18)** to a fresh local build of the current source — so the VPS was NOT serving stale code.
- Client code review: Login.tsx, Signup.tsx, GoogleAuthCallback.tsx all do `setAuth(...)` then `navigate('/dashboard')`; authStore.setAuth writes localStorage + sets `isAuthenticated: true` synchronously; ProtectedRoute only redirects when `isAuthenticated` is falsy. No component auto-redirects back to /login.

Conclusion: no reproducible failure exists in the current deployed code+backend for email login/signup at gentletap.co. The report most likely dates from the api-down window (when the API container was unhealthy) or a transient state. The one REAL defect class found: **access tokens expire after 15 minutes (`expires_in: 900`) and the 401 response interceptor was an empty stub** — so any session older than 15 min silently broke (all API calls 401 with no refresh/retry/logout), which users experience as "the site doesn't work after logging in".

### FIXES SHIPPED (this commit)
1. `frontend/src/lib/api.ts`: real 401 handling — single-flight refresh via `/auth/refresh`, retry original request once with the new token; on refresh failure call `logout()` so ProtectedRoute cleanly returns to /login. Added `apiErrorMessage()` helper that distinguishes network failures from API errors.
2. `frontend/src/pages/Login.tsx` + `Signup.tsx`: `navigate('/dashboard', { replace: true })` (back button no longer bounces to login) and clearer error banners ("Cannot reach the server…" vs "Invalid credentials").
3. Frontend rebuilt locally to verify compile (`dist/assets/index-BSX0ojUV.js`).

### If the owner STILL sees "stays on login" after deploying this
Ask for the exact red error text shown on the form and which URL they browse (domain vs VPS IP). If NO error appears and it truly silently stays: open DevTools → Network tab, redo login, check the status of `POST /api/v1/auth/login` and whether URL changes to `/dashboard` — report both.

### Recommended next debugging step (what was in progress)
Test the live auth flow first:
```
curl -s -X POST https://<deployed-host>/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"demo@gentletap.com","password":"password123"}'
```
✅ DONE — see results above.

---

## Phase 7 — DONE (final polish)

All planned phases and deferred follow-ups are now delivered. Nothing left in the queue except:
- **VPS secret rotation (owner action, cannot be done from here):** the Phase-8 audit scrubbed all leaked values from code, but they remain in git history AND may still be live on the VPS. Before/with the next deploy set these env vars (all fail-closed now — webhooks will 401 until set): `PADDLE_WEBHOOK_SECRET` (rotate in Paddle dashboard), `INTUIT_WEBHOOK_VERIFIER_TOKEN` (rotate in Intuit dev console), `FRESHBOOKS_WEBHOOK_VERIFIER_TOKEN`, `RESEND_WEBHOOK_SECRET`, `TWILIO_AUTH_TOKEN`, `ADMIN_API_KEY`, plus fresh `SECRET_KEY` / `JWT_SECRET_KEY` / `TOKEN_ENCRYPTION_KEY`. Then verify with: `docker compose run --rm api python scripts/security_check.py`.
  - Note: rotating TOKEN_ENCRYPTION_KEY/SECRET_KEY invalidates stored OAuth tokens only if they were encrypted with a derived key after this deploy; pre-existing plaintext tokens keep working and get re-encrypted on next refresh.
  - Known accepted npm audit items (need breaking major upgrades, assessed low risk): esbuild/vite dev-server advisory (dev-only, no prod exposure); react-router 6 advisories (SSR-focused — app has no SSR). Upgrade vite→8 + react-router→7 deliberately later.
- **Deploy:** `cd /opt/gentletap && git pull origin main && docker compose up -d --build`. All alembic migrations are idempotent and run automatically on API startup.
- Optional future ideas: per-affiliate resource page, intelligence recommendations surfaced inside the app UI (endpoint already exists), email-reply tracking for `client_responded_recently` (currently WhatsApp inbound + replied-status only), strict CSP header for nginx.

---

## Security reminders (Owner-was told)
- **Rotate on the VPS + in the Paddle dashboard:** the Paddle webhook secret and JWT/token-encryption keys were once committed. They are scrubbed from `.env.example` now, but they still exist in git history. Rotating them removes any exploit window.
- **ci caches** — if you change frontend deps (pnode versions), `npm ci` uses the lock file. Do NOT hand-edit `frontend/package.json` and `package-lock.json` independently; regenerate.

## Notes / gotchas for the next agent
- The API container **must not crash on startup**. The single highest-cost bug in this project was the `create_all` vs `alembic` re-create collision → whole stack aborts when api is unhealthy. If the api container is unhealthy, run `docker compose logs api | tail -50`.
- Keep every new alembic migration **idempotent**.
- The google auth callback page is wired, but `google_client_id` defaults empty and the code contains a `demo@gentletalk.com` fallback — do not push real Google creds into the repo.
- `frontend/nginx.conf` must keep the `/api/` proxy to `http://api:8000` and the SPA fallback `location /` → `/index.html`. Do not remove the security headers.
- The web container now serves on host port `127.0.0.1:3000`. The VPS nginx may proxy to that — confirm the host port did not change in the deployed environment.